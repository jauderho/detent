//! libdetent: cdylib + staticlib, cbindgen -> include/detent.h.
//!
//! `unsafe` is allowed in this crate only with a `// SAFETY:` comment
//! justifying the invariant being upheld.
#![deny(unsafe_code)]

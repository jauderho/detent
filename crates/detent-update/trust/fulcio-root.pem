PLACEHOLDER — no Fulcio root is embedded yet (crates/detent-update/trust/README.md).

Replace this file with the active Fulcio root CA(s) extracted from the current
Sigstore TUF snapshot, PEM `CERTIFICATE` blocks, before the first tagged
release. This placeholder deliberately does not parse as PEM: with no real
root the verifier refuses closed (VerificationError::TrustRootUnavailable).

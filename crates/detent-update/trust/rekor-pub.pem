PLACEHOLDER — no Rekor log public key is embedded yet
(crates/detent-update/trust/README.md).

Replace this file with the Rekor log public key extracted from the current
Sigstore TUF snapshot, PEM `PUBLIC KEY` (PKIX SPKI), ECDSA P-256, before the
first tagged release. This placeholder deliberately does not parse as PEM:
with no real key the verifier refuses closed
(VerificationError::TrustRootUnavailable).

[package]
name = "detent-modules"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Facade: registry of enabled modules (cfg(feature) per module)."
publish = false

[lints]
workspace = true

[dependencies]
detent-core = { path = "../detent-core" }
detent-module-hosts = { path = "../modules/hosts", optional = true }

[features]
default = []
## One feature per module crate; `detent`'s `module-<id>` feature forwards here.
module-hosts = ["dep:detent-module-hosts"]

[package]
name = "detent-module-network"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Network module: systemd-networkd, NetworkManager keyfiles, ifupdown, netplan, (BSD rc.conf)."
publish = false

[lints]
workspace = true

[dependencies]

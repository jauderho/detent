# /etc/resolv.conf — Debian default without a local resolver backend:
# the static resolver configuration an installer writes when no
# systemd-resolved, NetworkManager or unbound is managing the file.
nameserver 192.168.1.1
search lan
options ndots:2 edns0
